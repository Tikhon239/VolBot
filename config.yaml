POSTGRES:
  USER: !ENV ${PG_USER}
  PASSWORD: !ENV ${PG_PASSWORD}
  HOST: ec2-54-247-96-153.eu-west-1.compute.amazonaws.com
  PORT: 5432
  DATABASE: d4h4ekoeg3ddnl
  file_name: src/database/database_structure.sql
TG:
  TOKEN: !ENV ${TG_TOKEN}
VK:
  TOKEN: !ENV ${VK_TOKEN}
  METHOD: board.getTopics
  GROUP_ID: !ENV ${GROUP_ID}
  VERSION: 5.131